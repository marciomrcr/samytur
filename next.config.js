/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "cdn-icons-png.flaticon.com",
      "via.placeholder.com",
    ],
  },
};

module.exports = nextConfig;
